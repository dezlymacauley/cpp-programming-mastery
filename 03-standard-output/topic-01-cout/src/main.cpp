// This includes a header file called "iostream"
#include <iostream>

// This brings `cout` from `std` (the standard library) into scope.
// This allows you to use `cout` directly in this C++ file,
// rather than having to type out `std::cout` whenever you want to use it.
using std::cout;

int main() {

    cout << "C++ Programming Mastery\n";
    return 0;
}
